CLEARPATH MARKETING CONSOLE — Cloud Shell (correct method)

Do NOT paste giant Raw files into Cloud Shell.

────────────────────────────────────────
FIX file upload crash (ISO-8859-1 / Unicode filename)
+ Backend ONLINE + one owner login
────────────────────────────────────────

cd ~
rm -rf ~/CLAUDEDCLEAR-fix
gh auth status || gh auth login
gh repo clone clearpathcharts/CLAUDEDCLEAR ~/CLAUDEDCLEAR-fix -- --depth 1 --branch cursor/marketing-team-auth-bootstrap-2373
bash ~/CLAUDEDCLEAR-fix/scripts/marketingdept-bootstrap/FIX-CONTAINER-START.sh

Wait for SUCCESS.
Hard-refresh Incognito: https://fuckweasel.net
Login: owner
Pick the file again — Unicode / fancy filenames now upload.

If Latest still says old "Backend offline" on an old failed job:
  Open Queue → delete that "test" job, or ignore it and queue a new Discord-only post.

────────────────────────────────────────
Notes
────────────────────────────────────────
- Backend ONLINE + Discord ready = Dispatch Discord works now.
- telegram/youtube failed = those keys still incomplete (Telegram needs TELEGRAM_CHAT_ID).
- Do not paste giant scripts. Clone and run only.
