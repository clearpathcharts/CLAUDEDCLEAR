# Core AI Studio secrets for Clear Path Markets Science
GEMINI_API_KEY=
TWELVEDATA_API_KEY=

# Registration pipeline (waitlist + identity pre-registration)
# Firebase Admin JSON for server-side Firestore writes (duplicate checks, storage)
FIREBASE_SERVICE_ACCOUNT=
# SMTP for confirmation emails with activation keys
SMTP_HOST=
SMTP_PORT=587
SMTP_SECURE=false
SMTP_USER=
SMTP_PASS=
SMTP_FROM=ClearPath Trader <noreply@clearpathtrader.com>
ADMIN_NOTIFY_EMAIL=

# Cloud SQL PostgreSQL Database Credentials
SQL_HOST=
SQL_USER=
SQL_PASSWORD=
SQL_DB_NAME=
SQL_ADMIN_USER=
SQL_ADMIN_PASSWORD=

# Podcast Index (free at podcastindex.org — server-side only, never expose secret to browser)
PODCAST_INDEX_API_KEY=
PODCAST_INDEX_API_SECRET=
