# Copy to android/keystore.properties (gitignored) after creating your upload keystore.
# Generate keystore: npm run cpms:keystore
#
# storeFile=release.keystore
# storePassword=YOUR_STORE_PASSWORD
# keyAlias=clearpath_cpms
# keyPassword=YOUR_KEY_PASSWORD

storeFile=release.keystore
storePassword=
keyAlias=clearpath_cpms
keyPassword=
