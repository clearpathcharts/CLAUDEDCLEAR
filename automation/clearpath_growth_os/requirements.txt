# Pinned-loosely deps for environments that prefer pip over `crewai install`.
crewai[tools]>=0.150.0
requests>=2.31.0
pydantic>=2.6.0
python-dotenv>=1.0.0
# Native Gemini provider (uses the GEMINI_API_KEY you already have).
google-genai>=1.0.0
