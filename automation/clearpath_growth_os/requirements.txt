# Pinned-loosely deps for environments that prefer pip over `crewai install`.
# Do not use crewai[tools] — it pulls chromadb/litellm we do not need.
crewai>=0.150.0,<1.0.0
requests>=2.32.4
pydantic>=2.6.0
python-dotenv>=1.0.0
